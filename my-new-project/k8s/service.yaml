apiVersion: v1
kind: Service
metadata:
  name: route-analysis
  labels:
    app: route-analysis
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: route-analysis
